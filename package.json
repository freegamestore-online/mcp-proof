{
  "name": "mcp-proof",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @mcp-proof/web dev",
    "build": "pnpm --filter @mcp-proof/web build",
    "preview": "pnpm --filter @mcp-proof/web preview",
    "typecheck": "pnpm --filter @mcp-proof/web exec tsc -b",
    "test": "pnpm --filter @mcp-proof/web test"
  }
}
